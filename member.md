# Member 패키지 분석 및 정리

`com.passfail.member` 패키지는 사용자의 가입, 인증, 프로필 관리 및 계정 설정과 관련된 핵심 기능을 담당합니다.

## 1. 패키지 구조

- **controller**: HTTP 요청을 처리하고 뷰(Thymeleaf) 또는 JSON 응답을 반환합니다.
- **service**: 비즈니스 로직을 수행하며, 외부 API 연동 및 데이터 가공을 처리합니다.
- **repository**: 데이터베이스(JPA)에 접근하여 데이터를 조회하고 저장합니다.
- **dto**: 계층 간 데이터 전송을 위한 객체들로 구성됩니다.

## 2. 주요 기능

### A. 회원가입 및 인증
- **로컬 가입**: 아이디(7자 이상), 비밀번호(8자 이상, 특수문자 포함) 규칙 검증 후 가입 처리.
- **이메일 인증**: `MemberService`를 통해 6자리 인증 코드를 발송하고, 만료 시간(5분) 내 확인 과정을 거칩니다.
- **아이디 중복 확인**: 가입 전 실시간으로 아이디 가용성을 확인합니다.

### B. 소셜 로그인 및 연동 (`CustomOAuth2UserService`)
- **지원 제공자**: Google, Kakao, Naver, Github.
- **자동 계정 통합**: 소셜 이메일이 이미 로컬 계정으로 등록되어 있는 경우 자동으로 연동합니다.
- **베이스 멤버 시스템**: 소셜 전용 사용자의 경우, 연동 해제 시에도 계정이 유지될 수 있도록 해당 소셜 고유의 '베이스 멤버'를 생성하여 관리합니다.
- **커스텀 Principal**: `OAuth2Member`를 통해 Spring Security 컨텍스트에서 소셜 고유 ID 대신 DB의 닉네임을 사용하도록 래핑합니다.

### C. 마이페이지 및 프로필 (`MypageService`)
- **프로필 조회**: 사용자의 티어, 점수, 순위, 연동된 소셜 서비스 정보를 한눈에 보여줍니다.
- **활동 내역**: 최근 제출한 코드 내역 및 해결한 문제 목록을 페이징 처리하여 제공합니다.
- **알림 관리**: 본인인 경우에만 시스템 알림(댓글, 결과 등) 목록을 확인할 수 있습니다.

### D. 계정 관리
- **정보 수정**: 닉네임 변경 및 비밀번호 변경 기능을 제공합니다.
- **계정 복구 및 탈퇴**: 
  - **탈퇴(Withdraw)**: 물리적 삭제 대신 `isActive` 필드를 `false`로 변경하는 논리 삭제(Soft Delete) 방식을 사용합니다.
  - **복구(Reactivate)**: 비활성 상태로 로그인 시 복구 페이지로 유도하여 다시 활성화할 수 있습니다.
- **소셜 연동 해제**: 연동된 소셜 계정을 기본 '베이스 멤버'로 분리하여 연동을 해제합니다.

## 3. 주요 클래스 설명

| 클래스명 | 역할 |
| :--- | :--- |
| `MemberController` | 로그인, 회원가입, 비밀번호 찾기, 이메일 인증 등 일반 인증 요청 처리 |
| `MypageController` | 프로필 조회, 정보 수정, 계정 탈퇴 등 사용자 설정 요청 처리 |
| `MemberService` | 가입 로직, 이메일 발송, 비밀번호 검증, Spring Security 연동 |
| `MypageService` | 사용자 활동 데이터(제출, 문제풀이) 조회 및 정보 업데이트 로직 |
| `CustomOAuth2UserService` | OAuth2 로그인 성공 후 사용자 정보를 추출하고 DB와 동기화/연동 |
| `MemberInfoResponse` | 프로필 페이지에 필요한 모든 정보를 담아 뷰로 전달하는 DTO |

---
*이 문서는 코드 내 주석과 함께 `member` 패키지의 전반적인 이해를 돕기 위해 작성되었습니다.*
