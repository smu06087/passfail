# 리서치 보고서: Passfail 구현

## 1. 비즈니스 리서치
### "이게 돈이 될까? 시장성이 있는가?"
- **시장 규모**: 글로벌 이러닝 및 개발자 도구 시장은 빠르게 성장하고 있습니다. 경쟁 프로그래밍(Passfail)과 학습 자료, 프리미엄 문제 세트, 개발자 중심 굿즈를 결합한 플랫폼은 높은 잠재력을 가지고 있습니다.
- **경쟁사 분석**:
    - **LeetCode/HackerRank**: 프리미엄 구독 서비스를 제공합니다.
    - **인프런/Udemy**: 영상 강의 위주입니다.
    - **Passfail의 기회**: "코딩 배틀"과 커뮤니티 주도 콘텐츠라는 틈새 시장을 공략하여, "포인트 기반"의 독특한 쇼핑 경험을 제공할 수 있습니다.
- **트렌드 분석**: 학습의 "게이미피케이션(Gamification)". 배틀을 통해 획득한 포인트를 쇼핑몰에서 아이템이나 특전 구매에 사용하는 추세입니다.

## 2. 사용자 리서치
### "사람들이 정말 이걸 필요로 할까?"
- **타겟 유저**: 코딩 테스트를 준비하는 학생, 고급 도전에 목마른 개발자, 캐주얼한 경쟁을 즐기는 유저.
- **사용자 고충(Pain Points)**:
    - 코딩 성취에 대한 전문적인 온/오프라인 보상이 부족함.
    - 큐레이팅된 고품질 문제 세트를 찾기 어려움.
- **인터뷰/설문조사(가상)**: 유저들은 문제를 풀어 얻은 "포인트"를 프로필 꾸미기(티어, 스킨)나 실물 개발자 굿즈 구매에 사용하고 싶다는 의사를 보였습니다.

## 3. 기술 리서치
### "이걸 구현할 수 있는 기술이 있나?"
- **현재 기술 스택**:
    - **백엔드**: Spring Boot 3.3.4, Java 21, Spring Data JPA.
    - **보안**: Spring Security 6 (OAuth2 구글/소셜 로그인 지원).
    - **데이터베이스**: Oracle DB (ojdbc11), Redis (캐시/세션용).
    - **UI**: Thymeleaf + Spring Security Extras.
- **기존 기반 시설**:
    - `MemberEntity`에 이미 `pointBalance`가 존재함.
    - `PaymentEntity` 및 `PointTransactionEntity`가 이미 구현되어 있어 금융 거래를 위한 견고한 기반이 마련됨.
- **구현 전략**:
    - 기존 `MemberEntity`와 `PaymentEntity`를 재사용.
    - `ProductEntity`, `CategoryEntity`, `CartEntity`, `OrderEntity` 추가.
    - 성능 향상을 위해 Redis를 장바구니(Cart) 관리에 활용.
    - 현재 UI와 일관성을 유지하기 위해 Thymeleaf 서버 사이드 렌더링 사용.

## 4. 바이브 코딩 실패 현상 방지
1. **기존 레이어 일관성**: 이미 확립된 `controller -> service -> repository -> entity` 패턴을 엄격히 준수.
2. **ORM 관례**: `MemberEntity`와의 관계(주문 등) 설정 시 지연 로딩(Lazy Loading) 및 영속성 전이(Cascade) 타입을 적절히 처리.
3. **API 표준**: 엔드포인트 중복을 피하고 기존 명명 규칙을 따름 (예: `/api/v1/...` 또는 직접 컨트롤러 매핑).
