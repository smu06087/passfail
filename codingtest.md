# Codingtest 패키지 분석 및 정리

`com.passfail.codingtest` 패키지는 사용자가 작성한 코드를 온라인 환경에서 실행, 채점하고 AI 기반의 리뷰를 제공하는 코딩 테스트 핵심 기능을 담당합니다.

## 1. 패키지 구조

- **controller**: 코딩 테스트 에디터 화면 제공 및 코드 실행/제출/AI 리뷰 요청 API를 처리합니다.
- **service**: 실제 Java 코드를 로컬 환경에서 컴파일하고 샌드박스 형태로 실행하는 핵심 로직을 포함합니다.
- **dto**: 코드 실행 요청(커스텀 케이스) 및 실행 결과 데이터를 주고받기 위한 객체들로 구성됩니다.

## 2. 주요 기능

### A. 온라인 코드 에디터 (`CodingTestController`)
- **에디터 제공**: 사용자가 코드를 작성할 수 있는 Monaco Editor 기반의 환경에 필요한 문제 데이터와 초기 코드를 제공합니다.
- **상태 유지**: 이미 해결한 문제의 경우 이전에 제출했던 코드를 불러와 보여줍니다.

### B. 코드 샌드박스 실행 (`CodeExecutionService`)
- **임시 환경 구축**: `java.nio.file`을 사용하여 각 실행마다 독립적인 임시 디렉토리를 생성합니다.
- **컴파일 (`javac`)**: 작성된 코드를 `.java` 파일로 저장하고 프로세스를 생성하여 컴파일을 수행합니다.
- **제한된 실행 (`java`)**:
  - **메모리 제한**: `-Xmx` 옵션을 통해 JVM 최대 힙 메모리를 제한합니다.
  - **시간 제한**: `process.waitFor(timeout, unit)`를 사용하여 무한 루프나 과도한 실행 시간을 방지합니다.
- **결과 검증**: 표준 입력을 통해 테스트 케이스를 주입하고, 표준 출력을 캡처하여 기대값(Expected)과 비교합니다.

### C. 채점 및 제출
- **실행 (Run)**: 공식 테스트 케이스와 사용자가 직접 입력한 커스텀 테스트 케이스를 모두 실행하여 결과를 확인합니다.
- **제출 (Submit)**: 공식 테스트 케이스로만 채점을 수행하며, 모든 케이스를 통과할 경우 `ProblemService`를 통해 정답 처리 및 점수를 반영합니다.

### D. AI 코드 리뷰 연동
- **자동 리뷰**: 제출 결과 또는 현재 작성 중인 코드에 대해 AI(`AiCodeReviewService`)에게 분석을 요청하고, 개선 방향이나 시간 복잡도 등에 대한 피드백을 사용자에게 제공합니다.

## 3. 주요 클래스 설명

| 클래스명 | 역할 |
| :--- | :--- |
| `CodingTestController` | 에디터 페이지 로드, 코드 실행/제출/AI 리뷰 API 엔드포인트 관리 |
| `CodeExecutionService` | 로컬 프로세스를 이용한 Java 코드 컴파일 및 샌드박스 실행 로직 |
| `ExecutionResult` | 실행 상태(CORRECT, TIMEOUT 등), 출력값, 실행 시간 등을 담은 결과 DTO |
| `CustomTestCaseRequest` | 사용자가 에디터에서 직접 입력한 테스트 입출력값을 전달하는 DTO |

## 4. 실행 상태 코드 (Status Codes)

- **CORRECT**: 결과가 기대값과 일치함
- **WRONG**: 결과가 기대값과 일치하지 않음
- **TIMEOUT**: 설정된 시간 제한 내에 프로그램이 종료되지 않음
- **RUNTIME_ERROR**: 실행 중 예외(Exception)가 발생하거나 비정상 종료됨
- **COMPILE_ERROR**: 문법 오류 등으로 인해 컴파일에 실패함
- **SYSTEM_ERROR**: 파일 시스템 오류 등 서버 내부 문제로 실행 불가

---
*이 문서는 코드 내 주석과 함께 `codingtest` 패키지의 전반적인 이해를 돕기 위해 작성되었습니다.*
